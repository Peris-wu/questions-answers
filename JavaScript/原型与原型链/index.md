## 原型

\*\*=>\_\_

引用类型 都有一个隐式原型**proto**的属性，该属性的值指向了它的构造函数的显示原型 prototype 的属性值，该属性值是个普通对象。

## 原型链

如果访问一个对象的属性时，它会从自身去查找，如果没有查找到，它会去它的隐式原型**proto**查找，如果没有，它会再去它的隐式原型**proto**中查找（因为**proto**也是一个对象），通过层层查找 最后会到 Object.prototype 这个显示原型里。
